You are an expert full-stack QA engineer and senior developer. Your task is to perform a deep, comprehensive scan of the entire project codebase, focusing strictly and exclusively on the vendor Login and vendor Registration functionality. 

Do not modify or scan unrelated modules. Your goal is 0-bug production-readiness.

### 1. Scope of Scan
Analyze the entire project architecture to understand how vendor Login and Registration interact with all layers:
- UI/UX: Layout shifts, broken links, responsiveness, accessibility, form states (loading, disabled).
- Frontend: State management, API calls, event handlers, hooks, local storage/session handling.
- Backend: Authentication logic, JWT/session tokens, password hashing, error handling wrappers.
- Database: Schema alignment, queries, indexing for emails/phones, data types, connection leaks.
- Routes: Protected vs public routes, middleware sequence, redirect loops, HTTP methods.
- Validation: Frontend schemas (e.g., Zod, Yup) matching Backend validation (e.g., Joi, Express-validator). Check for edge cases, SQL injection, XSS, and regex flaws.
- Code Quality: Duplicate code, unused files, dead code, deprecated packages, race conditions.

### 2. Output Requirement
Create a single, comprehensive file named `vendor-login.md` in the root directory. This file must contain:
1. Executive Summary: Overall health of the vendor auth system.
2. Complete Bug Inventory: Categorized by severity (Critical, High, Medium, Low). Include the exact file paths and line numbers.
3. Code Issues: List of all duplicate files, dead code, or redundant logic found during the scan.
4. Step-by-Step Fix Guide: A professional, production-grade guide explaining exactly how to fix every single identified bug.

### 3. Execution Standard
- Do not hallucinate or skip any file that touches the vendor auth flow.
- Ensure the proposed fixes result in 0 errors and a fully functional, production-ready system.
- Wait for my approval after generating `vendor-login.md` before writing any code fixes.