Please perform a deep, comprehensive end-to-end audit and refactoring of the entire codebase (Frontend SPAs, Backend API Server, Routes, and Database/Migrations). 

Act as a Principal Software Architect and execute the following tasks with a highly structured and professional approach:

1. FULL-STACK DUPLICATION & BUG SCAN:
   - Scan all source files across the entire workspace to identify duplicate code blocks, redundant logic, obsolete/dead files, and unused functions.
   - Differentiate strictly between actual duplicates and intentional patterns (e.g., identical route names in separate frontend SPAs with different base paths are valid, but duplicate backend logic or orphaned files like dead router files must be flagged).

2. ROUTING & ARCHITECTURE REFACTORING:
   - Clean, streamline, and reorganize all backend and frontend routes into a clear, modular, and professional structure.
   - Group routing systems logically into well-defined sections (Infrastructure, Auth, Admin, Customer, Vendor, etc.) with explicit section headers.
   - Document every dual-mount or complex route (e.g., public vs. authenticated gating) using clear, explanatory inline comments explaining the exact business logic.

3. DATABASE & CLEANUP INTEGRATION:
   - Verify that all database schemas, migrations, and ORM query patterns match perfectly with the active backend routes and controllers.
   - Safely delete all confirmed dead code and orphaned files to optimize the workspace.

4. LOGICAL FIXES & WORKABLE CODE:
   - Fix all underlying logical bugs, syntax errors, and architectural flaws found during the investigation.
   - Ensure all refactored code maintains absolute functional integrity.

5. VERIFICATION & STRICT QUALITY CHECK:
   - Run a full TypeScript compilation check and ESLint verification on the modified codebase.
   - Ensure the output reaches exactly 0 errors and 0 warnings.
   - Restart the API server and guarantee a 100% clean startup with zero warnings, correct table loads, and running background schedulers.

Provide a detailed summary of your findings (Real Issues Found), the architectural changes applied, and the final verification state once the entire cleanup is fully functional.
